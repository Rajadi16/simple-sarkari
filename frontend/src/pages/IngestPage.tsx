/**
 * Ingestion page — URL and text ingestion forms (admin only).
 */

export default function IngestPage() {
  // TODO: URL ingestion form, text ingestion form, status display
  return (
    <div>
      <h1>Document Ingestion</h1>
      {/* TODO: URL form, Text form, Ingestion status list */}
    </div>
  );
}
