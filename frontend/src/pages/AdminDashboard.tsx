/**
 * Admin dashboard — source management, crawler triggers, job monitoring.
 */

export default function AdminDashboard() {
  // TODO: Source list, crawl triggers, job status overview
  return (
    <div>
      <h1>Admin Dashboard</h1>
      {/* TODO: <SourceList />, <JobList />, <CrawlRunStatus /> */}
    </div>
  );
}
